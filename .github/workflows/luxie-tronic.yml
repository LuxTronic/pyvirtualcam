# This file is generated by .github/luxie-workflows/scripts/generate.py.
# Do not edit directly; update .github/lux-ci-config.json and regenerate.

name: luxie-tronic
'on':
  pull_request_target:
    branches:
    - main
    types:
    - opened
    - reopened
    - synchronize
    - ready_for_review
    - review_requested
    - review_request_removed
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number to review
        required: true
        type: string
permissions:
  contents: write
  pull-requests: write
  issues: write
jobs:
  review:
    uses: LuxTronic/luxie-workflows/.github/workflows/reusable-luxie-tronic.yml@v1.6
    with:
      automation_ref: v1.6
      policy_path: .codex/luxie-tronic-policy.json
      pr_number: ${{ github.event.inputs.pr_number || '' }}
    secrets: inherit
